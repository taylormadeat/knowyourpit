kind = "web"
previewPath = "/"
title = "PitMaster AI"
version = "1.0.0"
id = "artifacts/pitmaster-ai"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24268

[services.development]
run = "pnpm --filter @workspace/pitmaster-ai run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pitmaster-ai", "run", "build" ]
publicDir = "artifacts/pitmaster-ai/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24268"
BASE_PATH = "/"
